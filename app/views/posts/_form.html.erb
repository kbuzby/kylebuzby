<%= form_for @post, html: { multipart: true } do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.select(:category, ['Car', 'Photos', 'Tech', 'VisualAtlas']) %><br/>
  <%= f.label :body %><br/>
  <%= f.text_area :body %>
  <%= f.submit "Save post" %>
<% end %>

